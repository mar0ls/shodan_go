version: "2"
run:
  timeout: 3m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - revive
    - unconvert
    - gocritic
    - gosec
    - nolintlint
  settings:
    gosec:
      # G705 (XSS via taint analysis) is a false positive for a CLI writing to os.Stdout/bytes.Buffer,
      # not to an HTTP ResponseWriter.
      # G706 (log injection) is a false positive for log.Printf calls with integer arguments (%d).
      excludes:
        - G705
        - G706
    revive:
      severity: warning
    gocritic:
      enabled-checks:
        - boolExprSimplify
  exclusions:
    rules:
      - path: "^generated_.*\\.go$"
        linters:
          - all
formatters:
  enable:
    - gofumpt
    - goimports
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
